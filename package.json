{
  "name": "design-scenes",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.33.3",
  "scripts": {
    "dev": "pnpm --filter @design-scenes/paper dev",
    "build": "pnpm -r --if-present build",
    "preview": "pnpm --filter @design-scenes/paper preview"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
