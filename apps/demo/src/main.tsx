import { bootstrap } from "oblik/host";

// oxlint-disable-next-line import/no-unassigned-import -- evaluating this module registers the practice composite tools (rect, boltCircle, ring, keyhole, bisect) for the Space palette
import "./layout/tools";

bootstrap();
